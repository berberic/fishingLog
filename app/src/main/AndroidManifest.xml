<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.berberic.android.fishinglog">

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="false"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/fish"
        android:label="@string/menu_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IntroActivity">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".FishlogHelpActivity"
            android:parentActivityName=".IntroActivity">
        </activity>
        <activity
            android:name=".FishlogImportExportActivity"
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:parentActivityName=".FishlogHelpActivity">
        </activity>
        <activity
            android:name=".FishlogListActivity"
            android:parentActivityName=".IntroActivity">
        </activity>
        <activity
            android:name=".FishlogPagerActivity"
            android:windowSoftInputMode="stateHidden">
        </activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.berberic.android.fishinglog.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/files"/>
        </provider>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"/>

        <activity
            android:name=".MapsActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".ListTitlesGoToActivity"
            android:parentActivityName=".FishlogListActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".LocationGoToActivity"
            android:parentActivityName=".FishlogListActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ListTitlesActivity"
            android:noHistory="true"

            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".LocationActivity"
            android:noHistory="true"

            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".MapsListActivity"
            android:parentActivityName=".IntroActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".MapsFind1Activity"
            android:noHistory="true"
            android:parentActivityName=".FishlogPagerActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".MapsFindNotesActivity"
            android:noHistory="true"
            android:parentActivityName=".FishlogPagerActivity"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>